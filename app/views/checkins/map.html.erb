<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
  <meta charset="utf-8">
  <title>Your Checkins</title>
  <style>
      html, body, #map-canvas {
          height: 100%;
          margin: 0px;
          padding: 0px
      }
  </style>
  <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDigiTGKOs8BUFxP6HqzXmum-F6WfXZGhQ&sensor=false"></script>
  <script>
      function initialize() {
          var myLatlng = new google.maps.LatLng(<%= @checkins_list.first[:latitude] %>,<%= @checkins_list.first[:longitude] %>);
          var mapOptions = {
              zoom: 7,
              center: myLatlng
          };
          var map = new google.maps.Map(document.getElementById('map-canvas'), mapOptions);
          new google.maps.Marker({
              position: myLatlng,
              map: map,
              title: "Hello World!"
          });
          <% @checkins_list.each do |checkin| %>
          new google.maps.Marker({
              position: new google.maps.LatLng(<%= checkin[:latitude] %>,<%= checkin[:longitude] %>),
              map: map,
              title: "<%= checkin[:venue] %>"
          });
          <% end %>
      }

      google.maps.event.addDomListener(window, 'load', initialize);
  </script>
</head>
<body>
<div id="map-canvas"></div>
</body>
</html>